#include "main.h"
/**
 * main - rint alphabets in lower and upper
 * Return: 0
 */
int main(void)
{
	char alpha:
	for (alpha = 'a'; alphan <= 'z'; alpha++)
		putcher(alpha);
	for (alpha = 'A'; alpha <= 'Z'; alpha++)
		putchar(alpha);
	putchar('\n');
	return (0);
}
